// test/widget_test.dart
// Basic smoke test untuk EventKuy - hanya verifikasi app bisa di-pump tanpa crash.

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test placeholder', (WidgetTester tester) async {
    // Placeholder — real integration tests akan ditambahkan terpisah.
    expect(true, isTrue);
  });
}
